class RecipeModel {
    constructor(id, name, ingredients, instructions, tips) {
      this.id = id;
      this.name = name;
      this.ingredients = ingredients;
      this.instructions = instructions;
      this.tips = tips;
    }
}

export default RecipeModel;
